LIBRARY ieee;
USE ieee.std_logic_1164.ALL;

ENTITY testbench IS
END testbench;

ARCHITECTURE tb OF testbench IS
  SIGNAL input  : std_logic_vector(43 DOWNTO 0);
  SIGNAL en     : std_logic := '1';
  SIGNAL clk    : std_logic := '0';
  SIGNAL output : std_logic_vector(54 DOWNTO 0);
BEGIN
  encoder : ENTITY work.encoder PORT MAP (input => input, en => en, clk => clk, output => output);

  clk <= NOT clk AFTER 10 ns;
  PROCESS
  BEGIN
    input(43 DOWNTO 40) <= "1010";
    WAIT FOR 25 ns;
  END PROCESS;


END tb;
